/*
package com.baidu.dingding.adapter;

import android.content.Context;


import com.baidu.dingding.R;
import com.baidu.dingding.adapter.adapterutil.ViewHolder;

import java.util.List;

*/
/**
 * Created by Administrator on 2015/11/13.RecyclerView 适配器
 *//*

public class ShopingAdapter extends AutoRVAdapter {
    public ShopingAdapter(Context context, List<?> list) {
        super(context, list);
    }

    @Override
    public int onCreateViewLayoutID(int viewType) {
        return R.layout.gouwu_gridview_chichun_item;
    }

    @Override
    public void onBindViewHolder(ViewHolder holder, int position) {
       */
/* LogUtil.i("RecyclerView适配器的position","position="+position);
        Map<String, Object> map = (Map<String, Object>) list.get(0);
        String[] str=new String[10];
        Iterator it = map.entrySet().iterator();
        for(int i=0;i<=map.size();i++) {
            Map.Entry entry = (Map.Entry) it.next();
            String key = entry.getKey().toString();
            str[i]=key;
        }
       attributeMyWidget=holder.getAttributeMyWidget(R.id.m_widght);
       attributeMyWidget.loadAttriData(str[position],map.get(position));*//*


    }
}
*/
